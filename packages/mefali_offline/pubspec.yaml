name: mefali_offline
description: Base de donnees locale Drift, sync queue et connectivity mefali
version: 0.0.1
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0
  flutter: '>=3.27.0'

dependencies:
  flutter:
    sdk: flutter
  mefali_core:
    path: ../mefali_core
  drift: ^2.22.0
  sqlite3_flutter_libs: ^0.6.0
  path_provider: ^2.1.0
  connectivity_plus: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.22.0
  build_runner: ^2.13.0
