import 'package:test/test.dart';
import 'package:mefali_api_client/mefali_api_client.dart';

// tests for DecisionSubstitution
void main() {
  final instance = DecisionSubstitutionBuilder();
  // TODO add properties to the builder and call build()

  group(DecisionSubstitution, () {
    // `true` = accepter le remplacement, `false` = le refuser (l'article est alors retiré, et rien n'est payé pour lui).
    // bool accepte
    test('to test the property `accepte`', () async {
      // TODO
    });

  });
}
