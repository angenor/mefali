# clients/ — clients API GÉNÉRÉS

Le contenu de `dart/` et `ts/` est produit par `scripts/generate-clients.sh`
à partir de `openapi.json`. **Ne jamais éditer à la main** (constitution I).
Régénéré et contrôlé en CI (job `contrat-clients`).
