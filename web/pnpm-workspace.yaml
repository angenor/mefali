# Configuration pnpm (pnpm 11 lit les réglages ici). Autorise les scripts de
# build des dépendances qui en ont besoin (esbuild pour vite/nuxt, etc.).
allowBuilds:
  '@parcel/watcher': true
  esbuild: true
  unrs-resolver: true
