import 'package:flutter_test/flutter_test.dart';

void main() {
  group('mefali_offline', () {
    test('package is importable', () {
      // Skeleton test — validates package structure is correct.
      // Real offline/drift tests will be added in subsequent stories.
      expect(true, isTrue);
    });
  });
}
