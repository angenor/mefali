# Shorebird — emplacement réservé (TRX-06, T4)

**HORS PÉRIMÈTRE du cycle 001-socle-monorepo** (constitution IX — « prêt ≠ construit »).

Mises à jour OTA du code Dart des apps Flutter (correctifs sans repasser par les
stores). Produit actif, standard OTA Flutter. Version notée pour référence :
Shorebird CLI 1.6.112 (Free : 5 000 patch installs/mois).

Aucune configuration ici tant que le cycle TRX-06 (tranche T4) n'est pas ouvert.
